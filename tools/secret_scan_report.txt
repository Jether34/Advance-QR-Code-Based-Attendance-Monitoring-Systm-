Heuristic secret scan report
Generated: 2025-12-10

Summary:
- Performed a repo-wide heuristic search for likely secret patterns (DB_PASS, API_KEY, student123, teacher123, TapIn2025, SECRET, PRIVATE_KEY).
- This is NOT a replacement for a proper secret scanner (detect-secrets/truffleHog). It finds obvious literals and examples.

Matches of note (representative):
- `quick_oracle_deploy.sh` (lines ~107-144): dynamically generates `DB_PASS` and echoes it to stdout. This script is intended for automated deployment and intentionally outputs credentials. Ensure the deployment environment captures or stores them securely (e.g., Vault) and does not log them publicly.
- `seed.php` (previously contained plaintext passwords like student123/teacher123) — this file was updated to generate random temporary passwords during seeding.
- `ORACLE_CLOUD_DEPLOYMENT.md` previously contained a literal example password (`TapIn2025XXXXX`) — replaced with a placeholder and instructions to use the deployment script output or secret store.
- `.env.example` contains `DB_PASS=your_secure_password_here` — this is acceptable for an example file.
- Several docs mention `DB_PASS`, `DB_USER`, etc. (deployment & security docs) — keep as placeholders, not real credentials.
- `db.php` and `api_config.php` correctly load DB credentials from environment variables (`getenv('DB_PASS')`).
- `.gitignore` includes common secret files like `secrets.txt` and `api_keys.txt` — good practice.

Recommendations:
1) Run a dedicated secret scanner on the repository and history: `detect-secrets` or `truffleHog`.
   - If secrets are found in commit history, plan a coordinated history rewrite (`git filter-repo` or BFG) and credential rotation.
2) Avoid echoing or writing credentials to logs in deployment scripts unless strictly necessary; if you must, ensure the file/console is captured in a secure vault or ephemeral channel.
3) Keep `.env` in `.gitignore` and provide `.env.example` only.
4) Consider integrating a pre-commit secret-scan hook (e.g., pre-commit with detect-secrets) to prevent future leaks.

Next steps performed in this run:
- `seed.php` updated to generate random temporary passwords and print them to console at seed time.
- `tools/final_security_audit.php` updated with a CLI-only guard to prevent web execution.
- `ORACLE_CLOUD_DEPLOYMENT.md` updated to remove a literal DB password and add guidance.

If you want, I can:
- (A) Attempt to run `detect-secrets` here and produce a full findings JSON (may require installing Python packages), or
- (B) Generate exact git commands and a remediation plan to scrub history and rotate credentials if secrets are found.

End of report.
